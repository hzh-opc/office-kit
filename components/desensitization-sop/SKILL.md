---
name: desensitization-sop
description: "在执行任何可能接触敏感信息的任务之前（任务输入含文本/文件/数据库/知识库/代码等任一形态），自动检测输入是否含敏感信息（个人标识、财务/审计/投研数据、密钥 Token 等）；若检出 → 先本地脱敏再执行任务；若未检出 → 忽略、直接执行任务（零额外负担）。此外，当任务涉及将本地内容送至云端大模型（WorkBuddy 云端模型、OpenClaw、Claude、Codex、GPT 等）处理时，须在「上云前」自动执行脱敏自查，在「任务结束后」自动生成审计汇总。本文件仅含执行所必需的最小规则；判定依据、分级对照表、精度影响、工具部署等详述见同目录 references/reference.md（按需读取，不自动加载；GitHub 项目说明见 README.md）。"
version: "2.11.1"
agent_created: true
---

# 信息脱敏上云 SOP（AI Agent 上云前 / 上云后闭环）

> **版本** v2.11.1（2026-08-23） · **署名** hzh.opc（Huang Zenghao，由 WorkBuddy 协助整理） · **版权** Copyright 2026 hzh.opc，基于 [Apache License 2.0](LICENSE) 发布（保留声明、标注修改、附 NOTICE）。

> **适用对象**：本技能主要面向**个人**与**企业**使用——含政府部门、事业单位、对数据安全有特别要求的企业等；此类组织 / 单位使用本技能时，**须遵循其适用的法律法规与内部数据安全管理规定**。无论哪类使用者，本技能均保证**全过程可追溯、可审计、可复核**。

核心方针（不可违背）：本地存储优先 · 最小必要 · 分类分级 · 脱敏后上云 · 可溯源 · 留审计 · 云端记忆禁存敏感信息。
**原始敏感文件永远留本地，绝不整份上传。**

## 一张图看懂流程

> 完整交互式指引见脚本内置 `desensitize.py guide`（含决策表与命令链的单一来源）。

**闸门决策表**（任务执行前先判定，任一项命中即按对应动作）：

| 判定条件 | 动作 | 护栏 / 留痕 |
|---|---|---|
| 仅本地处理 · 无需外发 | 不脱敏 | 三条护栏；外发即失效 + 多次外发每次检测 |
| 显式声明公开 / 已脱敏 | 不脱敏 | 可上云；留痕 + 警示（绝不静默） |
| 检出敏感 | run 脱敏 | preprocess → run → 自查 → 上云 → 审计 |
| 未检出 | 直接执行 | 零额外负担 |

**命令链**（推荐顺序）：`preprocess`（解密 / OCR）→ `run`（脱敏 + 映射表）→ `status`（回显索引）→ 上云（仅 `03_脱敏副本/`）→ `audit`（审计）→ `decrypt` / `restore`（本地复核 / 回填）。

## 触发与动作
- **触发条件 A（通用前置检测闸门）**：执行任何「任务输入可能接触敏感信息」的任务之前，先跑「输入检测闸门」。
- **触发条件 B（上云专项，强制）**：任何把本地内容送云端模型处理之前；任务结束自动生成审计。
- **动作零**：跑「输入检测闸门」（见下）。
- **动作一（上云前，强制）**：逐项核对「上云前自查清单（12 项）」，任一项不通过 → **中止上云并提示用户**。
- **动作二（任务后，自动）**：追加审计记录（模板见下）。

## 输入检测闸门（任何任务执行前的第一步）
目标：**凡任务输入可能含敏感信息，先检测、按需脱敏，再执行任务；无敏感则直接执行，零额外负担。**

按输入形态检测（均本地、离线、数据不出本机）：
- **文本 / 聊天输入**：落地为临时文件后 `desensitize.py scan`；或按 references/reference.md §2 三级风险表快速判定。
- **文件**：`desensitize.py scan <路径> --recursive`（覆盖文本/代码/Office/PDF/SQLite）。
- **数据库 / 查询结果**：先本地导出文本（SQL dump / CSV / `sqlite3 .dump`）再 `scan`，切勿原文外传。
- **知识库 / 网页抓取**：落地为文本后 `scan`；遵守"云端记忆/知识库禁存敏感原文"。

判定分支（对应上方决策表，详见「豁免与边界」）：
1. **仅本地处理 · 无需外发** → 不脱敏（数据不出本机，或云端取「方法」本地处理「数据」），须满足三条护栏；**外发即失效**。
2. **显式声明公开 / 已脱敏** → 跳过脱敏（`--assume-public` / `--public-paths`）。
3. **检出敏感** → `run --mode hybrid` 本地脱敏，用脱敏副本继续任务（映射表永不离开本地）。
4. **未检出** → 直接执行，零负担。

> 闸门只做"检测 + 按需脱敏"，不阻断正常任务；自动化识别非 100%（中文尤弱），脱敏后必须人工复核，禁止"一键脱敏即上云"。

## 动作一：上云前自查清单（12 项，逐项核对）
1. 原始敏感文件是否仍完整留本地、未整份上传？
2. 是否已逐份识别并标记敏感字段与级别（高/中/低，见 references/reference.md §2）？
3. 脱敏方法是否与级别匹配（高风险已剥离直接标识符 + 高风险准标识符）？
4. 脱敏副本是否仅来自 `desensitized/`（工作区为 `03_脱敏副本/`）目录？
5. 映射表是否与副本分离、已加密（`.desensitize_keys/`，工作区为 `04_映射表_保密/`，密钥不入库）？
6. **证券/投研**：是否拦截"内幕信息 / 未公开重大信息"？命中禁止上云。
7. **财务/审计**：是否落入"禁止输入公共 AI 平台"范围（底稿 / 涉密）？
8. **数值精度**：财务/持仓/金额是否保留精确值（仅去个人标识，未泛化/随机化）？
9. **代码**：是否检出并脱敏密钥/Token/API Key/账号密码，并经用户确认？
10. 云端记忆/知识库是否会被写入敏感原文？（必须禁存）
11. 是否需用户显式确认（拟上云的原始数据、含密钥代码等）？
12. **本地处理豁免**：声明「仅本地处理·无需外发」的未脱敏文件是否已隔离存放（`07_本地处理不外发/`）、绝不外发、未混入可上云目录？

> 数字对齐：本 12 项清单由 `05_上云前自检报告.md` 与 `audit` 生成的**九节审计文档合并承载**（清单 12 项 ≠ 审计 9 节，二者为"清单 → 审计"的多对一汇总，不要求逐项一一对应）。

**确认闸门（上云前的硬性步骤，不可跳过）**：`run --workspace` 会生成 `05_上云前自检报告.md`，AI Agent **必须**在上云前把其核心内容展示给用户并逐项确认：
1. 校对 OCR 待校对副本（`02_未脱敏副本/OCR待校对/`）；
2. 复核脱敏副本（`03_脱敏副本/`）；
3. 确认本地处理豁免留痕（`07_本地处理不外发/`）；
4. 确认公开豁免留痕与映射表保密。
**等待用户明确回复「确认上云」后**才可上传 `03_脱敏副本/`；禁止「一键脱敏即上云」。

## 动作二：审计汇总模板（追加到 工作区/desensitize_audit.md）
```
【脱敏审计记录】
- 任务时间 / 任务描述
- 原始数据：<本地路径> | 文件数 N | 体量 X
- 敏感字段与级别：<清单摘要>
- 脱敏方法：<遮蔽/假名/令牌/抑制/泛化…>
- 映射表：<重识别钥匙已加密封存、留本地（**路径不展示**，详见下方「对话内展示约定」）>
- 上云内容：<desensitized/ 路径，仅脱敏副本>
- 上云模型：<模型名>
- 外发记录（每次外发逐条；多次外发须逐次检测 + 记录）：
  - 外发 #1：<时间> | 内容：<文件/字段清单> | 脱敏状态：<已脱敏·沿用映射表 A / 新脱敏·映射表 B> | 上云模型
  - 外发 #2：<时间> | 内容：<…> | 脱敏状态：<…> | 上云模型
- 本地复核结论：<是否一致 / 差异说明>
- 精度影响评估：<无损 / 数值保留 / 已泛化需用户注意>
- 外泄风险自评：<低/中/高 + 理由>
- 操作人：<用户/AI> | 异常与待办：<若有>
```
留存：重要/核心数据相关日志 ≥ 1–3 年；建议每季度回看重评估。

## 对话内展示约定（脱敏完成卡片 + 安全红线）

脱敏产物以静态文件落盘（`成果索引.md` / `05_上云前自检报告.md` / `desensitize_report.json` / 审计记录），但 Agent 完成脱敏后，**必须在对话内输出结构化卡片**，让非专业人员无需逐个点开文件即可掌握「识别了哪些敏感信息 → 如何脱敏 → 哪些能外发」全貌。卡片与静态文件并存，卡片只承载「结果可信摘要」。

**安全红线（最高优先级，覆盖对话内一切回显）**：**映射表（重识别钥匙）是唯一含原值的文件，其路径本身即是攻击面**——对话内**既不回显原值，也不显示映射表的目录名或路径**。该红线不仅约束卡片，也覆盖 `status` 回显、成果索引、使用说明等一切可能把映射表路径带进对话的旁路。映射表的落盘位置仅在**本地审计文件**（`06_审计与回填/审计记录.md` 的「密钥与映射表」小节）内部交代，供本机复核，不进对话。

### ① 脱敏审计卡片（完成即输出）

```
【脱敏完成 · 审计摘要】
├─ 原始文件          → <本地路径>（未外发）
├─ 识别到敏感信息    → 共 N 处，分 X 类（见下方清单，仅脱敏后示例）
├─ 脱敏方式          → hybrid（语义掩码 + 唯一令牌，可逆）
├─ 可外发内容        → 仅 03_脱敏副本/（✅ 已脱敏）
├─ 保留本地内容      → 原始文件 / 本地处理原文（🚫 不外发；映射表已加密封存、留本地，路径不展示）
├─ 脱敏前后对照      → 见下方对照表（仅脱敏后示例，不含原值）
└─ 审计结果          → 风险自评：<低/中/高>（完整审计见过程文件）
```

### ② 敏感信息识别清单（脱敏后示例，严禁回显原值）

| 类型 | 数量 | 级别 | 示例（脱敏后） |
|------|------|------|----------------|
| 手机号 | 3 | 高 | 138****8000 |
| 身份证 | 1 | 高 | 4***************** |
| 姓名 | 2 | 中 | 张** |
| 银行卡 | 1 | 高 | 6222****... |

### ③ 外发边界清单（外发前二次确认）

```
【外发边界】
✅ 可外发（上云）     → 03_脱敏副本/ 目录下所有文件
🚫 保留本地（严禁外发）→ 原始文件 / 未脱敏副本 / 本地处理原文 / 重识别钥匙（已加密封存，不展示路径）
```
外发前把既有「确认闸门」拉到对话内：向用户二次确认「仅上传 03_脱敏副本/」后再执行。

### ④ 过程文件暂存约定（卡片下方附「不含原值」的路径）

卡片永远只给「少量示例」+「不含原值的过程文件路径」，完整过程以文件形式暂存、让用户按需点开；**映射表及其路径一律不上卡片**：

```
📁 过程文件（备查）：
├─ 完整审计     → <06_审计与回填/审计记录.md>
├─ 命中统计     → <desensitize_report.json>（仅统计计数，不含原值）
├─ 上云前自检   → <05_上云前自检报告.md>
└─ 成果索引     → <成果索引.md>
```

- 命中明细完整指向 `desensitize_report.json`（仅统计，不含原值）；
- 精确「原值 ↔ 令牌」对照**只存于加密映射表**，其落盘位置仅在审计记录的「密钥与映射表」小节交代，对话内不出现；
- 审计摘要卡片附「完整审计 → `06_审计与回填/审计记录.md`」（审计记录本身只含脱敏后形态与统计，不含原值明文）。

## 豁免与边界（用户显式声明才跳过脱敏，默认全脱敏）

v2.5.0 起**不再自动识别"公开主体"并豁免**（上市公司也有未公开/内幕信息），也**不再用文件名/目录名隐式推断**（`sample`/`demo`/`pub` 等命名会被误判）。**默认全脱敏，仅当用户显式声明时才跳过**。两种豁免语义不同，勿混用：

| 豁免类型 | 语义 | 是否脱敏 | 能否上云 | 声明通道 |
|---|---|---|---|---|
| 公开豁免 | 内容公开 | 不脱敏 | ✅ 可上云 | `--assume-public` / `--public-paths` / `.nodesens` 清单 |
| 本地处理豁免 | 内容保密、只本地处理 | 不脱敏 | 🚫 **严禁上云** | `--local-only` / `--local-paths` |

**公开豁免（声明为公开 / 样例 / 已脱敏）**：
- 提示词声明 → `--assume-public`（整体）；指定文件/文件夹 → `--public-paths`（文件夹=递归全部）；伴随清单 `.nodesens`/`--public-manifest`（重复使用）。
- 留痕 + 警示（含 size/sha256），scan 仍检测；会话级一次性；上市公司未公开信息红线不改（默认全脱）。

**本地处理·无需外发豁免（减负）**：对仅本地处理、无需外发的信息（数据不出本机；或云端取「方法」本地处理「数据」）不脱敏，直接用原始数据本地执行，省工作量、免精度损失。
- **三条护栏（强制，缺一不可，否则仍须脱敏）**：① 合理访问权限、无外泄风险；② 不得与外发脱敏副本一起存放（工作区自动隔离到 `07_本地处理不外发/`🚫）；③ 留痕 + 可审计。
- **外发即失效**：本地处理中追加/生成的数据如需外发，须重新 `scan` 检验 → 检出敏感则 `run` 脱敏 + 留痕（映射表支持 `restore` 回填）。所有外发数据必须确保已脱敏并记录。
- **多次外发（每次检测 + 记录）**：已脱敏的沿用原记录（复用映射表与审计、保持令牌一致），未脱敏的先补充脱敏记录再外发。
- **本地处理优先于公开**：同一文件同时命中两种声明时，按更严格的「本地处理（不外发）」处理。

## 本地预处理关卡（脱敏前必做 · 自动解密 / OCR）

加密文档、图片 / 图片型 PDF 若只在 `run` 末尾散落一句"跳过"，易漏脱敏。故前置为 `preprocess` 关卡：**本地解密 + 本地 OCR**，汇入**预处理确认单**（满足四点：① 原文件与未脱敏副本保存/外发清单；② OCR 逐字校对提醒；③ 异常清单；④ 确认闸门——`run --preprocess-manifest` 校验异常清完才放行）。全程离线、数据不出本机。技术细节（rapidocr/pikepdf/msoffcrypto-tool 选型与实现）见 references/reference.md §0.4。

## 统一成果中心（工作区 · 推荐非专业人员使用）

各阶段命令加 **`--workspace <目录>`** 后，产物归拢一处、清晰中文目录命名、附「可上云/保密」标签索引与上云前自检报告：

```
<工作区>/
├── 00_使用说明.md            # 各目录能否上云
├── 01_预处理确认单.md
├── 02_未脱敏副本/            # 🚫 保密（解密与原始副本/ + OCR待校对/）
├── 03_脱敏副本/              # ✅ 可上云：★唯一允许上传云端的目录
├── 04_映射表_保密/           # 🚫 保密：重识别钥匙，绝不随副本上传
├── 05_上云前自检报告.md      # 上云前必读：四处校对 + 确认闸门
├── 06_审计与回填/            # 审计记录.md + 回填成果/（🚫 保密）
├── 07_本地处理不外发/        # 🚫 不外发：仅本地处理未脱敏原文
└── 成果索引.json / 成果索引.md
```

随时 `desensitize.py status --workspace <目录>` 回显索引。不带 `--workspace` 保持非工作区默认行为（向后兼容）。

## 一键脱敏本地脚本

位置：`<技能目录>/scripts/desensitize.py`（uv 工程，`pyproject.toml` 声明依赖，数据全程不出本机；WorkBuddy 下 `<技能目录>` 即 `~/.workbuddy/skills/desensitization-sop`）。
- **解释器解析顺序**（便携，install / upgrade / 调用 / 测试一致）：`DESEN_PYTHON`（直接指定解释器，复用现有环境、不新建 venv）→ `DESEN_VENV`（指定 venv 目录）→ `<技能目录>/scripts/.venv`（install.py 默认创建）→ 系统 `python3`。
- 调用：`<技能目录>/scripts/.venv/bin/python <技能目录>/scripts/desensitize.py <子命令> ...`；若复用现有环境则 `DESEN_PYTHON=/path/to/python <技能目录>/scripts/desensitize.py <子命令> ...`。
- 快速指引：`... desensitize.py guide`（打印决策表与命令链）。

**8 个子命令速查表**（完整参数见 `... desensitize.py <子命令> --help`）：

| 子命令 | 作用 | 典型用法 |
|---|---|---|
| `guide` | 打印流程指引 + 闸门决策表 | `desensitize.py guide` |
| `scan` | 仅报告命中，不生成文件 | `desensitize.py scan <文件/目录> [--recursive]` |
| `preprocess` | 本地解密 + 本地 OCR + 预处理确认单 | `desensitize.py preprocess <文件/目录> [--recursive]` |
| `run` | 脱敏 + 加密映射表（核心，默认 hybrid） | `desensitize.py run <文件/目录> --out ./desensitized --keys ./.desensitize_keys` |
| `status` | 回显工作区成果索引 | `desensitize.py status --workspace <目录>` |
| `audit` | 自动生成九节审计文档 | `desensitize.py audit --report ./desensitize_report.json` |
| `decrypt` | 本地解密映射表、复核可逆性 | `desensitize.py decrypt --keys ./.desensitize_keys` |
| `restore` | 回填为含原值的本地内部文档 | `desensitize.py restore --keys ./.desensitize_keys --input ./desensitized --out ./restored` |

要点（完整参数与实现细节见 references/reference.md「配套一键脱敏本地脚本」）：
- `run` 默认 `--mode hybrid`（语义掩码+唯一令牌，可逆、无歧义、保留字段语义）；另有 `mask`/`token`/`redact`。
- 脱敏副本在 `--out`（默认 `./desensitized`，可上云）；映射表在 `--keys`（默认 `./.desensitize_keys`，密钥 600，绝不外发）。
- **豁免**：`--assume-public`/`--public-paths`/`--local-only`/`--local-paths`（见「豁免与边界」）。
- **识别增强**：`--names` 姓名清单、`--cn-enhance` 中文姓名/地址/机构名、`--tabular-names` 表格列头英文姓名（CSV/TSV/xlsx）；**生僻字/特殊字符优先 mask**；**`--mapping` 用户自定义映射**（原始值→替换值，命中主动精确替换，加密入库不外发）。
- **跨境识别（v2.8+ 内置）**：IBAN / SWIFT / VAT / 国际电话自动识别脱敏；多编码自动探测（GBK / Shift-JIS / BIG5 / cp1252）；全角数字归一；csv/json 内嵌密钥检测。
- **能力边界（英文/多语言姓名）**：表格 CSV/TSV/xlsx 的英文姓名用 `--tabular-names`（列头解析，John Smith → J*** S****）；自由文本（docx 段落/邮件）与泰文/越南文等多语言姓名，**NER 模型暂不引入**（需联网下载模型、违背「离线·最小依赖」原则），用 `--names` 名单召回。详见 references/reference.md。
- **清洗建议**：检测疑似未清洗形态（分隔/短位手机、15 位旧证、订单号与银行卡区间重叠），只提醒不自动改。

## CLI 用法（便携版 S1 / 纯命令行调用）

本组件在 S1 便携版部署下只有 CLI 身份（`components/desensitization-sop/scripts/desensitize.py`），SKILL.md 的「触发词 / 技能用法」对纯 CLI 用户无用，CLI 用户直接以脚本为入口：

```bash
# 解释器解析（便携顺序：DESEN_PYTHON > DESEN_VENV > scripts/.venv > 系统 python3）
PY="<技能目录>/scripts/.venv/bin/python"    # 或复用现有环境：export DESEN_PYTHON=/path/to/python
$PY <技能目录>/scripts/desensitize.py <子命令> ...   # 见上「8 个子命令速查表」

# 已并入 office-kit 套件时，走统一入口（等价）
python3 ~/office-kit/kit.py desen <子命令> ...
```

## 双副本入口优先级（套件已装时）

本组件已并入 office-kit 套件（元技能）。当本机**同时**存在以下两份副本时，**以套件副本为权威**：

| 副本 | 路径 | 何时生效 |
|---|---|---|
| 套件权威副本 | `~/office-kit/components/desensitization-sop/` | 已装 office-kit 套件时（S2 / S3），走 `kit.py desen` |
| 独立副本 | `~/.workbuddy/skills/desensitization-sop/` | 仅 S4「仅组件」场景（未装套件）生效 |

- 已装套件时，请走统一入口 `python3 ~/office-kit/kit.py desen <子命令>`，避免双副本版本漂移、入口优先级混淆。
- 独立安装器（`install.py`）内置双副本守卫：检测到套件副本时提示「双副本风险」，引导走 `kit.py desen` 或 `--force` 明确覆盖。

## 手动升级（安全零停机 · 仅手动触发）

升级**默认不自动运行**，仅在用户明确要求时手动执行。仓库自带 `upgrade.py`（纯标准库），流程：下载到暂存区 → 暂存副本校验（`scan→run→decrypt→restore` 全环）→ 备份 + 原子替换 → 线上再实测（失败自动回滚）。用法：`python3 upgrade.py [--check|--dry-run|--force|--source local|--clean-backup]`。详见 README「升级」节。

## 支持的信息源与上云前注意事项

脚本覆盖文本/代码（.txt/.md/.csv/.json/.xml/.html/.log/.rst/.eml/.py/.sql 等）、Office（.docx/.xlsx/.pptx）、PDF、SQLite（.db）。加密文档、图片 / 图片型 PDF 等由 `preprocess` 自动解密 / OCR 并汇入确认单，绝不静默放过。

> 红线重申：脱敏副本可上云，原始文件与映射表留本地且分离；密码、加密原文件、已解密未脱敏副本、原始图片/OCR 文本同样严禁外传；自动化识别非 100%，禁止"一键脱敏即上云"，必须人工复核。

## 判定与方法的快速索引（详述见 references/reference.md，按需 Read）
- 输入检测闸门（按输入形态检测 + 分支决策；0.4 预处理关卡、0.5 显式声明豁免、0.6 本地处理·无需外发豁免）：§0
- 关键概念（去标识化 vs 匿名化、准标识符重识别）：§1
- 三级风险判定表、准标识符聚合风险：§2
- 六步脱敏流程、映射表安全条款：§3
- 精度影响（"去标识不扭曲数值"）+ 方法对照表：§4
- 财务/审计/投研/代码场景专项要求：§5
- 工具选型、技术五族、静态/动态脱敏：§6
- 配套一键脱敏本地脚本（完整参数、生僻字/特殊字符、--mapping、清洗建议、能力边界）：§6 之后
- 应急与改进、附录（直接标识符/敏感个人信息速查）：§7
