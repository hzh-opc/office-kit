# office-kit 本地配置样例（复制为 config/.env 后按本机填写）
# ⚠️ config/.env 含密钥，已被 .gitignore 排除，绝不入库、绝不进对话。
#
# 加载方式（2026-09-12 起生效）：kit.py 启动时自动读取 config/.env，把 KEY=VALUE 补进环境变量。
#   · 只补「当前未设置」的变量 —— 显式 export 的同名项永远优先，绝不覆盖用户意图；
#   · 容忍注释（#）、空行、`export ` 前缀、值两端引号；写错的行静默跳过，不会中断运行；
#   · 注入后随子进程继承，故经 kit.py 分发的组件命令都能读到；
#   · 组件被直接裸跑（不经 kit.py）时不加载，此时请显式 export 或改用 kit.py 分发。
#   自定义路径：OFFICE_KIT_ENV_FILE=/path/to/your.env ；查看生效状态：python kit.py doctor

# 工作台根目录（默认 <office-kit>/workbench，按业务流程阶段组织；无需写死绝对路径，仅在确需自定义时开启）
# OFFICE_KIT_WORKBENCH=workbench

# 显式指定工具包根目录（跨机器迁移时若目录名变化可在此覆盖自动探测）
# OFFICE_KIT_ROOT=~/office-kit

# ── 虚拟环境「单一来源」约定（2026-09-11）─────────────────────────────────
# 全套件（含各组件与测试工具）复用已部署生产副本 ~/office-kit/.venv；开发副本与其他
# 本地副本不再自建 .venv，杜绝「一份组件依赖、多个 venv」的重复与漂移。
# 解析顺序：OFFICE_KIT_VENV > <本副本>/.venv > <OFFICE_KIT_ROOT>/.venv > ~/office-kit/.venv
# 一般无需在此设置；确需覆盖时开启下一行：
# OFFICE_KIT_VENV=~/office-kit/.venv

# ── 大模型与密钥「单一来源」约定（2026-09-11）──────────────────────────────
# 只在本文件登记；开发副本 / 测试工具不得另建自己的模型配置，避免「同一能力、多份凭据」漂移。
#
# 1) 本地模型（info-extract 画面解读 VLM / 文本自动纠正）—— 走 ollama REST，离线可用：
# OLLAMA_HOST=http://localhost:11434
# INFO_EXTRACT_VISION_TIER=1        # 0 轻量3B / 1 标准7B / 2 高性能 / 3 旗舰32B
#   ⚠ 需本机已装 ollama 并 pull 对应模型；未装时 info-extract 自动降级为「仅 OCR 文字 + 提示」。
#
# 2) 云端 TTS（summarize podcast.py --tts；须先过 DESEN 外发闸门，命中敏感先阻断）：
# OPENAI_API_KEY=
# AZURE_SPEECH_KEY=
# AZURE_SPEECH_REGION=
#
# 3) 模型权重 / 缓存为**用户级共享**（无需逐副本重复下载，也勿在项目内另建缓存目录）：
#       ollama       → ~/.ollama/models
#       HuggingFace  → ~/.cache/huggingface
#
# 4) 环境变量优先级：显式 export > config/.env > 代码默认。

# 组件升级/修复（kit.py check/upgrade/repair）的远程源，默认公开仓库 hzh-opc/office-kit @ main。
# 如需私有仓库/镜像可覆盖（下载走 GitHub raw + codeload tarball）：
# OFFICE_KIT_REPO=owner/repo
# OFFICE_KIT_BRANCH=main

# 本地密钥文件路径（预留位：当前没有任何组件读取该变量，等需要本地鉴权的组件接入时再启用）
# HZH_AGENT_KEY_PATH=~/Documents/hzh-agent-key
