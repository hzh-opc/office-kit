# office-kit 本地配置样例（复制为 .env 后按本机填写，.env 不入库）
# 工作台根目录（默认 <office-kit>/workbench，按业务流程阶段组织；无需写死绝对路径，仅在确需自定义时开启）
# OFFICE_KIT_WORKBENCH=workbench

# 本地密钥文件路径（info-extract 等需鉴权组件读取；请勿写入对话/仓库）
HZH_AGENT_KEY_PATH=~/Documents/hzh-agent-key

# 可选：显式指定工具包根目录（跨机器迁移时若目录名变化可在此覆盖自动探测）
# OFFICE_KIT_ROOT=~/office-kit

# 组件升级/修复（kit.py check/upgrade/repair）的远程源，默认公开仓库 hzh-opc/office-kit @ main。
# 如需私有仓库/镜像可覆盖（下载走 GitHub raw + codeload tarball）：
# OFFICE_KIT_REPO=owner/repo
# OFFICE_KIT_BRANCH=main

# ── 虚拟环境 / 大模型「单一来源」约定（2026-09-11）─────────────────────────
# ⚠️ 现状提示：**当前没有任何代码加载 config/.env**（全仓库无 dotenv / .env 读取逻辑），
#    故本文件目前只是「声明式清单」而非生效配置——密钥/模型实际仍需靠显式 export
#    或在组件调用处传参。是否补一个统一的 .env 加载器，见工作区
#    `规划文档/office-kit_全面检查报告_20260911.md`（待你决定）。
#
# 1) 虚拟环境：全套件（含各组件与测试工具）**复用已部署生产副本 ~/office-kit/.venv**，
#    开发副本与其他本地副本不再自建 .venv。可在此显式覆盖（一般不必）：
# OFFICE_KIT_VENV=~/office-kit/.venv
#
# 2) 大模型与密钥：**约定**只在本文件（复制为 config/.env）登记，开发副本/测试工具
#    不得另建自己的模型配置，避免「同一能力、多份凭据/多个模型」漂移。
#    · 本地模型（info-extract 画面解读 VLM / 文本纠正）走 ollama REST（**已生效**，读环境变量）：
# OLLAMA_HOST=http://localhost:11434
# INFO_EXTRACT_VISION_TIER=1        # 0 轻量3B / 1 标准7B / 2 高性能 / 3 旗舰32B
#    · 云端 TTS（summarize podcast.py --tts，须先过 DESEN 外发闸门；**已生效**，读环境变量）：
# OPENAI_API_KEY=
# AZURE_SPEECH_KEY=
# AZURE_SPEECH_REGION=
#    · 模型权重缓存为**用户级共享**（无需逐副本重复下载）：
#       ollama  ~/.ollama/models · HuggingFace ~/.cache/huggingface
#
# 3) 环境变量优先级：显式 export > （未来）config/.env > 代码默认。
