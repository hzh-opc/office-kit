# 信息脱敏上云 SOP —— scripts 运行时依赖
# 由 install.py 自动安装到 venv（默认 <技能目录>/scripts/.venv，可用 --venv/DESEN_VENV 指定目录，或 --python/DESEN_PYTHON 复用现有解释器）
# Python 版本：标准 CPython >=3.10 且 <3.14（onnxruntime 无 free-threaded wheel）
cryptography>=42.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
pypdfium2>=4.30.0          # PDF 文本抽取 + 页面渲染（Chrome 同款 PDFium，C 实现快且稳健）
pikepdf>=9.11.0            # 本地解密 PDF（封装 QPDF，稳健；运行时依赖）
msoffcrypto-tool>=6.0.0    # 本地解密 Office 文档（运行时依赖）
rapidocr>=3.9.0            # 本地 OCR：模型随 wheel 捆绑，完全离线、数据不出本机
onnxruntime>=1.19.0        # rapidocr 推理后端
charset_normalizer>=3.0.0  # 编码探测（cp932/Shift-JIS、big5、cp1252、gb18030 等；纯 Python、离线）
# 注意：headless Linux 服务器上 opencv-python 需系统库 libgl1（apt-get install -y libgl1）
